"""Expande ERP endpoints. GET /api/erp/order/{order_id} looks up a specific
order by ID via the Playwright ERP agent. GET /api/erp/search?customer=name
searches orders by customer name."""
from __future__ import annotations
