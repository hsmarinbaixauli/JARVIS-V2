"""Tool call dispatcher. Maps Claude tool names to their handler functions and
executes them with the injected services context. Replaces the
_dispatch_tool_call() function from the legacy src/main.py. Includes stub
branches for erp_get_order_status and erp_search_by_customer."""
from __future__ import annotations


def dispatch():
    pass
