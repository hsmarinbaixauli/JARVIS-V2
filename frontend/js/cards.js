/**
 * Rich tool-result card renderers. Step 8.
 *
 * Each function returns a DOM element to insert into the chat,
 * or null if the data doesn't warrant a card (fall back to plain text).
 */

export function renderCalendarCard(_data) { return null; }
export function renderEmailCard(_data)    { return null; }
export function renderErpOrderCard(_data) { return null; }
export function renderErpSearchCard(_data) { return null; }
